import { Content, Header, Layout } from '../../common/Layout';

type Props = {};
export const GlobalNav = ({}: Props) => {
  return (
    <Layout>
      <Header></Header>
      <Content></Content>
    </Layout>
  );
};
